# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files — every variant, so a stray
# .env.production or .env.staging can't slip through
.env
.env.*
!.env.example

# credentials, in case one is ever copied into the tree for debugging
credentials.json
*.pem
*.key

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# CI / build artifacts
*.tsbuildinfo

# Local agent tooling
.pi

# Demo recording: rendered audio and video.
demos/build
