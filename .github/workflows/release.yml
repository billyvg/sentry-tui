name: Release

# Tag-driven: `git tag v0.1.0 && git push origin v0.1.0`. The tag must match
# `version` in package.json — the first job fails the run otherwise.
#
# Run it by hand first (workflow_dispatch, dry_run: true) to build and package
# everything without publishing anything.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Build and package, but publish nothing"
        type: boolean
        default: true

env:
  SKIP_INSTALL_SIMPLE_GIT_HOOKS: "1"
  BUN_VERSION: "1.3.1"

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  verify:
    name: Verify version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      dry_run: ${{ steps.version.outputs.dry_run }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Resolve and check version
        id: version
        env:
          REF_NAME: ${{ github.ref_name }}
          IS_TAG: ${{ github.ref_type == 'tag' }}
          DRY_RUN_INPUT: ${{ inputs.dry_run }}
        run: |
          version="$(node -p "require('./package.json').version")"
          echo "version=$version" >> "$GITHUB_OUTPUT"

          if [ "$IS_TAG" = "true" ]; then
            if [ "$REF_NAME" != "v$version" ]; then
              echo "::error::tag $REF_NAME does not match package.json version $version"
              exit 1
            fi
            echo "dry_run=false" >> "$GITHUB_OUTPUT"
          else
            echo "dry_run=${DRY_RUN_INPUT:-true}" >> "$GITHUB_OUTPUT"
          fi

  build:
    name: Build ${{ matrix.target }}
    needs: verify
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      # Kept in step with scripts/release-targets.ts by scripts/packaging.test.ts.
      matrix:
        include:
          - target: darwin-arm64
            runner: macos-latest
          - target: darwin-x64
            runner: macos-15-intel
          - target: linux-x64
            runner: ubuntu-latest
          - target: linux-arm64
            runner: ubuntu-24.04-arm
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build binary
        run: bun run ./scripts/build-bin.ts --target ${{ matrix.target }}

      # Ad-hoc signature. arm64 macOS refuses to exec an unsigned binary, and a
      # binary that travelled through a tarball has no signature of its own.
      - name: Sign (macOS)
        if: runner.os == 'macOS'
        run: codesign --sign - --force dist/bin/${{ matrix.target }}/sentry-tui

      - name: Smoke test
        run: dist/bin/${{ matrix.target }}/sentry-tui --help

      - name: Upload binary
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.target }}
          path: dist/bin/${{ matrix.target }}/sentry-tui
          if-no-files-found: error
          retention-days: 7

  release:
    name: Package and publish
    needs: [verify, build]
    runs-on: ubuntu-latest
    permissions:
      contents: write # create the GitHub Release
      id-token: write # npm provenance
    env:
      VERSION: ${{ needs.verify.outputs.version }}
      DRY_RUN: ${{ needs.verify.outputs.dry_run }}
      # `secrets` is not readable from a step-level `if`, so fold it into env here.
      HAS_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN != '' }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Download binaries
        uses: actions/download-artifact@v7
        with:
          path: dist/bin

      - name: Archive binaries and checksums
        run: .github/scripts/package-release.sh

      - name: Assemble npm packages
        run: bun run build:npm --strict

      - name: Generate Homebrew formula
        run: bun run build:formula

      # Uses the npm that ships with the runner image; provenance needs the
      # id-token permission granted above, not a particular Node version.
      - name: Publish to npm
        if: env.DRY_RUN != 'true'
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: .github/scripts/publish-npm.sh

      - name: Create GitHub Release
        if: env.DRY_RUN != 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "v${VERSION}" \
            --repo "${GITHUB_REPOSITORY}" \
            --title "v${VERSION}" \
            --generate-notes \
            dist/release/*

      - name: Update Homebrew tap
        if: env.DRY_RUN != 'true' && env.HAS_TAP_TOKEN == 'true'
        env:
          GH_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
        run: .github/scripts/update-homebrew-tap.sh

      - name: Upload release bundle (dry run)
        if: env.DRY_RUN == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: release-bundle
          path: |
            dist/release
            dist/npm
            dist/homebrew
          retention-days: 7
